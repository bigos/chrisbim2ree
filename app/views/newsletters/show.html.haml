%p#notice= notice

%div.newsletter{:style => 'max-width: 600px; margin: 0 auto;'}
  %p    
    = raw @newsletter.content

- if current_admin    
  = blink_to 'Edit', edit_newsletter_path(@newsletter) 
  \|
  = blink_to 'Back', newsletters_path
