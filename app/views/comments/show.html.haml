%p#notice= notice

%p
  %b Post:
  = @comment.post_id
%p
  %b Content:
  = @comment.content

= blink_to 'Back', post_path(@comment.post)
= blink_to 'Edit', edit_comment_path(@comment) if current_admin
= blink_to 'Comments', comments_path if current_admin
