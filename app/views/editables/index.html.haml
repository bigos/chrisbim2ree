%h3 Listing editables

%table
  %tr
    %th Name
    %th Content
    %th
    %th
    %th

  - @editables.each do |editable|
    %tr
      %td= editable.name
      %td= editable.content
      %td= blink_to 'Show', editable
      %td= blink_to 'Edit', edit_editable_path(editable)
      %td= blink_to 'Destroy', editable, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= blink_to 'New Editable', new_editable_path
